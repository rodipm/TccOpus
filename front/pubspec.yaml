name: front
description: Interface para sistemas de integração

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'
  
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  http: ^0.13.4
  universal_html: ^2.0.8
  flutter_dotenv: ^5.0.2
  url_launcher: ^6.0.20
  flutter_typeahead: ^3.2.4
  auto_size_text: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter


# The following section is specific to Flutter.
flutter:
  uses-material-design: true

  assets:
    - assets/
    - .env